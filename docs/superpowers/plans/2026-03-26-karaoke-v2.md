# K歌 v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v1 伴奏生成基础上，增加歌名显示、带时间轴歌词滚动、浏览器录音、以及人声+伴奏混音导出。

**Architecture:** Flask 后端新增歌词获取（YouTube字幕 → Whisper fallback）和 FFmpeg 混音接口；前端用 Web Audio API 录音，录音结束后将 WebM 音频 POST 给后端，后端完成混音返回下载链接。

**Tech Stack:** Python/Flask, yt-dlp (字幕), openai-whisper (本地转写), ffmpeg (混音), Web Audio API / MediaRecorder (浏览器录音)

---

## File Map

| 文件 | 操作 | 职责 |
|------|------|------|
| `app.py` | Modify | 新增 `/lyrics/<job_id>`、`/mix/<job_id>` 路由；`/process` 返回歌名 |
| `karaoke.py` | Create | 歌词获取逻辑：YouTube字幕解析 → Whisper fallback |
| `templates/index.html` | Modify | 歌名展示、歌词滚动面板、录音控制 UI、混音下载 |
| `tests/test_karaoke.py` | Create | 歌词模块单元测试 |
| `tests/test_app.py` | Create | Flask 路由集成测试 |

---

## Task 1: 歌词模块 — YouTube 字幕获取

**Files:**
- Create: `karaoke.py`
- Create: `tests/test_karaoke.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_karaoke.py
import pytest
from pathlib import Path
from karaoke import parse_vtt, LyricLine

def test_parse_vtt_basic():
    vtt = """WEBVTT

00:00:01.000 --> 00:00:04.000
Hello world

00:00:05.000 --> 00:00:08.000
Second line
"""
    lines = parse_vtt(vtt)
    assert len(lines) == 2
    assert lines[0].start == 1.0
    assert lines[0].end == 4.0
    assert lines[0].text == "Hello world"
    assert lines[1].start == 5.0

def test_parse_vtt_empty():
    lines = parse_vtt("WEBVTT\n\n")
    assert lines == []
```

- [ ] **Step 2: 跑测试确认失败**

```bash
venv/bin/python -m pytest tests/test_karaoke.py -v
```
Expected: `ImportError` 或 `ModuleNotFoundError`

- [ ] **Step 3: 实现 karaoke.py 歌词解析**

```python
# karaoke.py
import re
from dataclasses import dataclass
from pathlib import Path
from typing import List, Optional
import subprocess
import sys


@dataclass
class LyricLine:
    start: float   # seconds
    end: float
    text: str


def _timestamp_to_seconds(ts: str) -> float:
    """'00:01:23.456' or '01:23.456' -> float seconds"""
    parts = ts.strip().replace(",", ".").split(":")
    if len(parts) == 3:
        h, m, s = parts
        return int(h) * 3600 + int(m) * 60 + float(s)
    elif len(parts) == 2:
        m, s = parts
        return int(m) * 60 + float(s)
    return float(parts[0])


def parse_vtt(content: str) -> List[LyricLine]:
    """解析 WebVTT 字幕内容，返回 LyricLine 列表"""
    lines = []
    blocks = re.split(r"\n\n+", content.strip())
    pattern = re.compile(
        r"(\d[\d:,.]+)\s+-->\s+(\d[\d:,.]+)\s*\n([\s\S]+)"
    )
    seen_texts = set()
    for block in blocks:
        m = pattern.search(block)
        if not m:
            continue
        start = _timestamp_to_seconds(m.group(1))
        end = _timestamp_to_seconds(m.group(2))
        text = re.sub(r"<[^>]+>", "", m.group(3)).strip()
        text = re.sub(r"\s+", " ", text)
        if not text or text in seen_texts:
            continue
        seen_texts.add(text)
        lines.append(LyricLine(start=start, end=end, text=text))
    return lines


def fetch_youtube_subtitles(url: str, job_dir: Path) -> Optional[List[LyricLine]]:
    """用 yt-dlp 下载字幕（优先手动字幕，fallback 自动字幕）"""
    for auto in [False, True]:
        cmd = [
            sys.executable, "-m", "yt_dlp",
            "--skip-download",
            "--write-subs" if not auto else "--write-auto-subs",
            "--sub-format", "vtt",
            "--sub-langs", "zh-Hans,zh-Hant,zh,en",
            "--output", str(job_dir / "subtitle.%(ext)s"),
            "--quiet",
            url,
        ]
        subprocess.run(cmd, capture_output=True)
        for f in job_dir.glob("subtitle*.vtt"):
            content = f.read_text(encoding="utf-8", errors="ignore")
            lines = parse_vtt(content)
            if lines:
                return lines
    return None


def transcribe_with_whisper(audio_path: Path) -> List[LyricLine]:
    """用本地 Whisper medium 模型转写，返回带时间轴歌词"""
    import whisper
    model = whisper.load_model("medium")
    result = model.transcribe(str(audio_path), word_timestamps=False)
    lines = []
    for seg in result.get("segments", []):
        text = seg["text"].strip()
        if text:
            lines.append(LyricLine(
                start=seg["start"],
                end=seg["end"],
                text=text,
            ))
    return lines


def get_lyrics(url: str, audio_path: Path, job_dir: Path) -> List[LyricLine]:
    """主入口：YouTube字幕 → Whisper fallback"""
    lines = fetch_youtube_subtitles(url, job_dir)
    if lines:
        return lines
    return transcribe_with_whisper(audio_path)
```

- [ ] **Step 4: 跑测试确认通过**

```bash
venv/bin/python -m pytest tests/test_karaoke.py -v
```
Expected: 2 tests PASSED

- [ ] **Step 5: Commit**

```bash
git add karaoke.py tests/test_karaoke.py
git commit -m "feat: 歌词模块 - VTT解析 + YouTube字幕获取 + Whisper fallback"
```

---

## Task 2: 安装 Whisper 依赖

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: 安装 openai-whisper**

```bash
uv pip install --python venv/bin/python openai-whisper
```

- [ ] **Step 2: 验证安装**

```bash
venv/bin/python -c "import whisper; print('whisper ok')"
```
Expected: `whisper ok`

- [ ] **Step 3: 更新 requirements.txt**

在 requirements.txt 末尾加：
```
openai-whisper>=20231117
```

- [ ] **Step 4: Commit**

```bash
git add requirements.txt
git commit -m "deps: 添加 openai-whisper"
```

---

## Task 3: Flask 后端 — /process 返回歌名和歌词

**Files:**
- Modify: `app.py`
- Create: `tests/test_app.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_app.py
import json
import pytest
from pathlib import Path
from app import app as flask_app

@pytest.fixture
def client():
    flask_app.config["TESTING"] = True
    with flask_app.test_client() as c:
        yield c

def test_health(client):
    resp = client.get("/health")
    assert resp.status_code == 200
    data = json.loads(resp.data)
    assert data["status"] == "ok"

def test_process_no_input(client):
    resp = client.post("/process")
    assert resp.status_code == 400
    data = json.loads(resp.data)
    assert "error" in data

def test_lyrics_not_found(client):
    resp = client.get("/lyrics/nonexistent")
    assert resp.status_code == 404
```

- [ ] **Step 2: 跑测试确认状态**

```bash
venv/bin/python -m pytest tests/test_app.py -v
```
Expected: test_health PASS, test_process_no_input PASS, test_lyrics_not_found FAIL (路由不存在)

- [ ] **Step 3: 修改 app.py — /process 返回歌名，后台线程获取歌词，新增 /lyrics 路由**

在 `app.py` 的 process 路由里做以下修改：

1. 下载后立刻保存 meta.json（歌名）
2. 分离完成后，用 `threading.Thread` 在后台异步获取歌词（YouTube字幕或Whisper），写入 lyrics.json
3. `/process` 立即返回，不等歌词完成
4. 前端通过轮询 `/lyrics/<job_id>` 获取歌词（未完成时返回 202）

**修改 `download_audio` 函数**，使其同时返回标题（用 `--print-json` 拿 metadata）：

```python
def download_audio(url: str, job_dir: Path):
    """用 yt-dlp 下载音频，返回 (文件路径, 标题)"""
    import json as _json
    # 先获取 metadata（不下载）
    meta_cmd = [
        sys.executable, "-m", "yt_dlp",
        "--skip-download", "--print-json", "--quiet", "--no-playlist", url,
    ]
    meta_result = subprocess.run(meta_cmd, capture_output=True, text=True)
    title = url  # fallback
    if meta_result.returncode == 0 and meta_result.stdout.strip():
        try:
            title = _json.loads(meta_result.stdout)["title"]
        except Exception:
            pass

    outtmpl = str(job_dir / "source.%(ext)s")
    cmd = [
        sys.executable, "-m", "yt_dlp",
        "--format", "bestaudio/best",
        "--output", outtmpl,
        "--no-playlist", "--quiet", url,
    ]
    subprocess.run(cmd, check=True, capture_output=True, text=True)
    for f in job_dir.iterdir():
        if f.stem == "source":
            return f, title
    raise FileNotFoundError("yt-dlp 下载后找不到音频文件")
```

**修改 `process()` 路由**，使用新的返回值，后台线程获取歌词：

```python
import json as _json
import threading

def _fetch_and_save_lyrics(url: str, audio_path: Path, job_dir: Path):
    """后台线程：获取歌词并写入 lyrics.json。url 为空时直接走 Whisper。"""
    try:
        from karaoke import get_lyrics
        lyrics = get_lyrics(url if url else None, audio_path, job_dir)
        data = [{"start": l.start, "end": l.end, "text": l.text} for l in lyrics]
    except Exception:
        data = []
    (job_dir / "lyrics.json").write_text(_json.dumps(data, ensure_ascii=False))

# process() 路由中替换原有的 url 分支处理：
if url:
    audio_path, title = download_audio(url, job_dir)  # 现在返回 (path, title)
elif uploaded_file and uploaded_file.filename:
    suffix = Path(uploaded_file.filename).suffix.lower()
    if suffix not in ALLOWED_EXTENSIONS:
        shutil.rmtree(job_dir, ignore_errors=True)
        return jsonify({"error": f"不支持的文件格式: {suffix}"}), 400
    audio_path = job_dir / f"source{suffix}"
    uploaded_file.save(str(audio_path))
    title = uploaded_file.filename
else:
    shutil.rmtree(job_dir, ignore_errors=True)
    return jsonify({"error": "请输入 YouTube 链接或上传音频文件"}), 400

accompaniment_path = separate_vocals(audio_path, job_dir)

threading.Thread(
    target=_fetch_and_save_lyrics,
    args=(url, audio_path, job_dir),
    daemon=True
).start()

return jsonify({
    "job_id": job_id,
    "title": title,
    "accompaniment_url": f"/output/{job_id}/accompaniment.mp3",
    "lyrics_url": f"/lyrics/{job_id}",
})
```

新增 `/lyrics/<job_id>` 路由（lyrics.json 未就绪时返回 202）：

```python
@app.route("/lyrics/<job_id>")
def serve_lyrics(job_id):
    safe_id = Path(job_id).name
    job_dir = OUTPUTS_DIR / safe_id
    if not job_dir.exists():
        return jsonify({"error": "任务不存在"}), 404
    lyrics_path = job_dir / "lyrics.json"
    if not lyrics_path.exists():
        return jsonify({"status": "processing"}), 202
    import json as _json
    return jsonify(_json.loads(lyrics_path.read_text()))
```

同时更新 `karaoke.py` 的 `get_lyrics` 签名，允许 url 为 None：

```python
def get_lyrics(url: Optional[str], audio_path: Path, job_dir: Path) -> List[LyricLine]:
    """主入口：YouTube字幕（url不为空时）→ Whisper fallback"""
    if url:
        lines = fetch_youtube_subtitles(url, job_dir)
        if lines:
            return lines
    return transcribe_with_whisper(audio_path)
```

- [ ] **Step 4: 跑测试确认全部通过**

```bash
venv/bin/python -m pytest tests/test_app.py -v
```
Expected: 3 tests PASSED

- [ ] **Step 5: Commit**

```bash
git add app.py tests/test_app.py
git commit -m "feat: /process 返回歌名和歌词URL，新增 /lyrics 路由"
```

---

## Task 4: Flask 后端 — /mix 混音接口

**Files:**
- Modify: `app.py`
- Modify: `tests/test_app.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_app.py` 末尾追加：

```python
def test_mix_missing_job(client):
    data = {"audio": (b"fake", "vocal.webm")}
    resp = client.post("/mix/nonexistent",
                       data={"audio": (b"fake audio data", "vocal.webm")},
                       content_type="multipart/form-data")
    assert resp.status_code == 404
```

- [ ] **Step 2: 跑测试确认失败**

```bash
venv/bin/python -m pytest tests/test_app.py::test_mix_missing_job -v
```
Expected: FAIL (路由不存在，404 或 405)

- [ ] **Step 3: 实现 /mix 路由**

在 `app.py` 新增混音路由：

```python
@app.route("/mix/<job_id>", methods=["POST"])
def mix_audio(job_id):
    safe_id = Path(job_id).name
    job_dir = OUTPUTS_DIR / safe_id
    accompaniment = job_dir / "accompaniment.mp3"
    if not accompaniment.exists():
        return jsonify({"error": "任务不存在"}), 404

    vocal_file = request.files.get("audio")
    if not vocal_file:
        return jsonify({"error": "未上传录音文件"}), 400

    vocal_path = job_dir / "vocal_raw.webm"
    vocal_file.save(str(vocal_path))

    # webm -> wav
    vocal_wav = job_dir / "vocal.wav"
    subprocess.run([
        "ffmpeg", "-y", "-i", str(vocal_path),
        "-ar", "44100", "-ac", "2", str(vocal_wav)
    ], check=True, capture_output=True)

    # 混音：伴奏 + 人声，人声音量稍微提升 1.5x
    mixed_path = job_dir / "mixed.mp3"
    subprocess.run([
        "ffmpeg", "-y",
        "-i", str(accompaniment),
        "-i", str(vocal_wav),
        "-filter_complex", "[0:a][1:a]amix=inputs=2:duration=longest:weights=1 1.5[out]",
        "-map", "[out]",
        "-codec:a", "libmp3lame", "-b:a", "192k",
        str(mixed_path)
    ], check=True, capture_output=True)

    return jsonify({"mixed_url": f"/output/{job_id}/mixed.mp3"})


@app.route("/output/<job_id>/mixed.mp3")
def serve_mixed(job_id):
    safe_id = Path(job_id).name
    mp3_path = OUTPUTS_DIR / safe_id / "mixed.mp3"
    if not mp3_path.exists():
        return jsonify({"error": "文件不存在"}), 404
    return send_file(str(mp3_path), mimetype="audio/mpeg", as_attachment=False)


@app.route("/download/<job_id>/mixed.mp3")
def download_mixed(job_id):
    safe_id = Path(job_id).name
    mp3_path = OUTPUTS_DIR / safe_id / "mixed.mp3"
    if not mp3_path.exists():
        return jsonify({"error": "文件不存在"}), 404
    return send_file(str(mp3_path), mimetype="audio/mpeg", as_attachment=True,
                     download_name="my_karaoke.mp3")
```

- [ ] **Step 4: 跑测试确认通过**

```bash
venv/bin/python -m pytest tests/test_app.py -v
```
Expected: 4 tests PASSED

- [ ] **Step 5: Commit**

```bash
git add app.py tests/test_app.py
git commit -m "feat: /mix 混音接口 + /output/<id>/mixed.mp3 下载"
```

---

## Task 5: 前端 — 歌名 + 歌词滚动面板 + 录音控制

**Files:**
- Modify: `templates/index.html`

- [ ] **Step 1: 完整替换 `templates/index.html`**

写入以下完整文件内容：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>屋内K歌</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #0f0f1a; color: #e0e0f0;
      min-height: 100vh; display: flex;
      align-items: center; justify-content: center; padding: 20px;
    }
    .card {
      background: #1a1a2e; border-radius: 16px; padding: 40px;
      width: 100%; max-width: 600px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.4);
    }
    h1 { font-size: 1.6rem; margin-bottom: 8px; color: #c084fc; }
    .subtitle { font-size: 0.9rem; color: #888; margin-bottom: 32px; }
    label { display: block; font-size: 0.85rem; color: #aaa; margin-bottom: 6px; }
    input[type="url"], input[type="file"] {
      width: 100%; padding: 12px 14px; background: #0f0f1a;
      border: 1px solid #333; border-radius: 8px; color: #e0e0f0;
      font-size: 0.95rem; outline: none; transition: border-color 0.2s;
    }
    input[type="url"]:focus { border-color: #c084fc; }
    .divider {
      text-align: center; color: #444; margin: 20px 0; font-size: 0.85rem; position: relative;
    }
    .divider::before, .divider::after {
      content: ""; position: absolute; top: 50%; width: 42%; height: 1px; background: #333;
    }
    .divider::before { left: 0; } .divider::after { right: 0; }
    button[type="submit"] {
      width: 100%; padding: 14px; margin-top: 24px;
      background: #7c3aed; color: white; border: none;
      border-radius: 8px; font-size: 1rem; font-weight: 600;
      cursor: pointer; transition: background 0.2s;
    }
    button[type="submit"]:hover { background: #6d28d9; }
    button[type="submit"]:disabled { background: #4a4a6a; cursor: not-allowed; }
    #status { margin-top: 28px; display: none; }
    .spinner { display: flex; align-items: center; gap: 12px; color: #aaa; font-size: 0.9rem; }
    .spinner-ring {
      width: 22px; height: 22px; border: 3px solid #333;
      border-top-color: #c084fc; border-radius: 50%;
      animation: spin 0.8s linear infinite; flex-shrink: 0;
    }
    @keyframes spin { to { transform: rotate(360deg); } }
    /* 歌曲区域 */
    .song-title { font-size: 1.1rem; font-weight: 600; color: #c084fc; margin-bottom: 14px; }
    /* 歌词面板 */
    .lyrics-panel {
      background: #0f0f1a; border-radius: 10px; padding: 16px;
      height: 200px; overflow-y: auto; margin-bottom: 16px;
      scroll-behavior: smooth;
    }
    .lyric-line {
      padding: 6px 8px; border-radius: 6px; font-size: 0.95rem;
      color: #666; transition: all 0.3s; cursor: default;
    }
    .lyric-line.active { color: #c084fc; font-weight: 600; background: #1a1a2e; }
    .lyrics-loading { color: #555; font-size: 0.85rem; font-style: italic; }
    /* 播放器 */
    audio { width: 100%; margin-bottom: 14px; accent-color: #c084fc; }
    /* 录音控制 */
    .karaoke-controls { display: flex; gap: 10px; margin-bottom: 14px; }
    .btn {
      flex: 1; padding: 12px; border: none; border-radius: 8px;
      font-size: 0.95rem; font-weight: 600; cursor: pointer; transition: background 0.2s;
    }
    .btn-start { background: #059669; color: white; }
    .btn-start:hover { background: #047857; }
    .btn-stop { background: #dc2626; color: white; }
    .btn-stop:hover { background: #b91c1c; }
    .btn:disabled { background: #4a4a6a; cursor: not-allowed; }
    /* 混音区域 */
    .mix-section { background: #0f0f1a; border-radius: 10px; padding: 16px; margin-top: 14px; }
    .mix-title { font-size: 0.85rem; color: #888; margin-bottom: 10px; }
    .action-btn {
      display: inline-block; padding: 10px 20px;
      background: #1e1e3a; border: 1px solid #7c3aed;
      color: #c084fc; border-radius: 8px; text-decoration: none;
      font-size: 0.9rem; transition: background 0.2s; cursor: pointer;
      border: none; width: 100%; margin-top: 10px;
    }
    .action-btn:hover { background: #2a2a4a; }
    .download-btn {
      display: inline-block; padding: 10px 20px; margin-top: 10px;
      background: #1e1e3a; border: 1px solid #7c3aed;
      color: #c084fc; border-radius: 8px; text-decoration: none; font-size: 0.9rem;
    }
    .download-btn:hover { background: #2a2a4a; }
    .error-box {
      background: #2a1010; border: 1px solid #7f1d1d; border-radius: 8px;
      padding: 14px; color: #fca5a5; font-size: 0.85rem; white-space: pre-wrap;
    }
    .rec-indicator {
      display: inline-block; width: 10px; height: 10px;
      background: #dc2626; border-radius: 50%; margin-right: 6px;
      animation: blink 1s infinite;
    }
    @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.2} }
  </style>
</head>
<body>
  <div class="card">
    <h1>屋内K歌</h1>
    <p class="subtitle">粘贴 YouTube 链接或上传音频，自动生成无人声伴奏</p>

    <form id="mainForm">
      <label for="urlInput">YouTube 链接</label>
      <input type="url" id="urlInput" name="url" placeholder="https://www.youtube.com/watch?v=..." />
      <div class="divider">或</div>
      <label for="fileInput">上传本地音频</label>
      <input type="file" id="fileInput" name="file" accept=".mp3,.wav,.m4a,.flac,.ogg,.aac" />
      <button type="submit" id="submitBtn">生成伴奏</button>
    </form>

    <div id="status"></div>
  </div>

  <script>
    const form = document.getElementById('mainForm');
    const statusDiv = document.getElementById('status');
    const submitBtn = document.getElementById('submitBtn');

    let currentJobId = null;
    let lyrics = [];
    let lyricsPollingTimer = null;
    let mediaRecorder = null;
    let recordingChunks = [];
    let recordingBlob = null;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const url = document.getElementById('urlInput').value.trim();
      const file = document.getElementById('fileInput').files[0];
      if (!url && !file) { showError('请输入 YouTube 链接或上传音频文件'); return; }

      submitBtn.disabled = true;
      showSpinner('正在处理，M1 芯片大约需要 1-3 分钟，请稍候...');

      const formData = new FormData();
      if (url) formData.append('url', url);
      if (file) formData.append('file', file);

      try {
        const resp = await fetch('/process', { method: 'POST', body: formData });
        const data = await resp.json();
        if (!resp.ok || data.error) { showError(data.error || '未知错误'); }
        else { showKaraokeUI(data); }
      } catch (err) {
        showError('网络错误: ' + err.message);
      } finally {
        submitBtn.disabled = false;
      }
    });

    function showKaraokeUI(data) {
      currentJobId = data.job_id;
      statusDiv.style.display = 'block';
      statusDiv.innerHTML = `
        <div class="song-title">${escapeHtml(data.title)}</div>
        <div class="lyrics-panel" id="lyricsPanel">
          <div class="lyrics-loading">歌词加载中...</div>
        </div>
        <audio id="audioPlayer" src="${escapeHtml(data.accompaniment_url)}" preload="auto"></audio>
        <div class="karaoke-controls">
          <button class="btn btn-start" id="btnStart" onclick="startKaraoke()">🎤 开始唱歌</button>
          <button class="btn btn-stop" id="btnStop" onclick="stopKaraoke()" disabled>⏹ 停止录音</button>
        </div>
        <div id="mixSection"></div>
      `;

      const player = document.getElementById('audioPlayer');
      player.addEventListener('timeupdate', () => highlightLyric(player.currentTime));

      startLyricsPolling(data.lyrics_url);
    }

    // 歌词轮询
    function startLyricsPolling(url) {
      if (lyricsPollingTimer) clearInterval(lyricsPollingTimer);
      lyricsPollingTimer = setInterval(async () => {
        try {
          const resp = await fetch(url);
          if (resp.status === 202) return; // 还在处理
          if (!resp.ok) { clearInterval(lyricsPollingTimer); return; }
          const data = await resp.json();
          if (Array.isArray(data)) {
            lyrics = data;
            clearInterval(lyricsPollingTimer);
            renderLyrics();
          }
        } catch(e) { clearInterval(lyricsPollingTimer); }
      }, 3000);
    }

    function renderLyrics() {
      const panel = document.getElementById('lyricsPanel');
      if (!panel) return;
      if (!lyrics.length) {
        panel.innerHTML = '<div class="lyrics-loading">未找到歌词</div>';
        return;
      }
      panel.innerHTML = lyrics.map((l, i) =>
        `<div class="lyric-line" data-index="${i}">${escapeHtml(l.text)}</div>`
      ).join('');
    }

    function highlightLyric(currentTime) {
      if (!lyrics.length) return;
      let active = -1;
      for (let i = 0; i < lyrics.length; i++) {
        if (currentTime >= lyrics[i].start) active = i;
        else break;
      }
      document.querySelectorAll('.lyric-line').forEach((el, i) => {
        const wasActive = el.classList.contains('active');
        el.classList.toggle('active', i === active);
        if (i === active && !wasActive) {
          el.scrollIntoView({ behavior: 'smooth', block: 'center' });
        }
      });
    }

    // 录音
    async function startKaraoke() {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
        mediaRecorder = new MediaRecorder(stream);
        recordingChunks = [];
        mediaRecorder.ondataavailable = e => { if (e.data.size > 0) recordingChunks.push(e.data); };
        mediaRecorder.start(100);

        const player = document.getElementById('audioPlayer');
        player.currentTime = 0;
        player.play();

        document.getElementById('btnStart').disabled = true;
        document.getElementById('btnStop').disabled = false;
        document.getElementById('mixSection').innerHTML =
          '<div style="color:#aaa;font-size:0.85rem"><span class="rec-indicator"></span>录音中...</div>';
      } catch (err) {
        showError('无法访问麦克风: ' + err.message);
      }
    }

    function stopKaraoke() {
      const player = document.getElementById('audioPlayer');
      player.pause();

      if (mediaRecorder && mediaRecorder.state !== 'inactive') {
        mediaRecorder.stop();
        mediaRecorder.stream.getTracks().forEach(t => t.stop());
        mediaRecorder.onstop = () => {
          recordingBlob = new Blob(recordingChunks, { type: 'audio/webm' });
          showMixButton();
        };
      }
      document.getElementById('btnStart').disabled = false;
      document.getElementById('btnStop').disabled = true;
    }

    function showMixButton() {
      document.getElementById('mixSection').innerHTML = `
        <button class="action-btn" onclick="uploadMix()">🎵 生成混音版本</button>
      `;
    }

    async function uploadMix() {
      if (!recordingBlob || !currentJobId) return;
      document.getElementById('mixSection').innerHTML =
        '<div class="spinner"><div class="spinner-ring"></div><span>混音处理中...</span></div>';

      const fd = new FormData();
      fd.append('audio', recordingBlob, 'vocal.webm');

      try {
        const resp = await fetch(`/mix/${currentJobId}`, { method: 'POST', body: fd });
        const data = await resp.json();
        if (!resp.ok || data.error) {
          document.getElementById('mixSection').innerHTML =
            `<div class="error-box">${escapeHtml(data.error || '混音失败')}</div>`;
        } else {
          document.getElementById('mixSection').innerHTML = `
            <div class="mix-section">
              <div class="mix-title">混音完成</div>
              <audio controls src="${escapeHtml(data.mixed_url)}"></audio>
              <a class="download-btn" href="/download/${escapeHtml(currentJobId)}/mixed.mp3" download>
                下载混音 MP3
              </a>
            </div>`;
        }
      } catch (err) {
        document.getElementById('mixSection').innerHTML =
          `<div class="error-box">网络错误: ${escapeHtml(err.message)}</div>`;
      }
    }

    function showSpinner(msg) {
      statusDiv.style.display = 'block';
      statusDiv.innerHTML = `<div class="spinner"><div class="spinner-ring"></div><span>${msg}</span></div>`;
    }

    function showError(msg) {
      statusDiv.style.display = 'block';
      statusDiv.innerHTML = `<div class="error-box">${escapeHtml(msg)}</div>`;
    }

    function escapeHtml(s) {
      return String(s)
        .replace(/&/g,'&amp;').replace(/</g,'&lt;')
        .replace(/>/g,'&gt;').replace(/"/g,'&quot;');
    }
  </script>
</body>
</html>
```

- [ ] **Step 2: 手动验证（自动测试覆盖路由，UI 需目视）**

```bash
# 启动服务
venv/bin/python app.py &
# 检查首页正常返回
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5000/
```
Expected: 200

- [ ] **Step 3: Commit**

```bash
git add templates/index.html
git commit -m "feat: 前端歌名+歌词滚动+录音控制+混音下载"
```

---

## Task 6: 端到端集成测试

**Files:**
- Modify: `tests/test_app.py`

- [ ] **Step 1: 完整流程测试（使用测试链接）**

```bash
# 启动服务
pkill -f "python app.py"; sleep 1
venv/bin/python app.py &
sleep 2

# 提交 YouTube 链接（My Little Airport 测试歌曲）
curl -s -X POST http://127.0.0.1:5000/process \
  -F "url=https://www.youtube.com/watch?v=8kla1Swx5tU" \
  --max-time 600
```

Expected 输出示例：
```json
{"accompaniment_url":"/output/XXXX/accompaniment.mp3","job_id":"XXXX","lyrics_url":"/lyrics/XXXX","title":"歌曲名称"}
```

- [ ] **Step 2: 验证歌词接口**

```bash
JOB_ID=<上一步返回的 job_id>
curl -s http://127.0.0.1:5000/lyrics/$JOB_ID | python3 -m json.tool | head -20
```

Expected: JSON 数组，每项含 `start`, `end`, `text`

- [ ] **Step 3: 验证混音接口（用静音 webm 测试）**

```bash
# 生成 3 秒静音 webm 作为模拟录音
ffmpeg -y -f lavfi -i anullsrc=r=44100:cl=stereo -t 3 -c:a libopus /tmp/test_vocal.webm 2>/dev/null

curl -s -X POST http://127.0.0.1:5000/mix/$JOB_ID \
  -F "audio=@/tmp/test_vocal.webm" | python3 -m json.tool
```

Expected: `{"mixed_url": "/output/XXXX/mixed.mp3"}`

- [ ] **Step 4: 验证混音文件可访问且有合理大小**

```bash
curl -s -o /dev/null -w "%{http_code} %{size_download}" \
  http://127.0.0.1:5000/output/$JOB_ID/mixed.mp3
```

Expected: `200 <大于 100000 的字节数>`

- [ ] **Step 5: 最终 commit**

```bash
pkill -f "python app.py"
git add app.py karaoke.py templates/index.html requirements.txt tests/
git commit -m "v2.0: K歌完整版 - 歌名+歌词+录音+混音"
```
